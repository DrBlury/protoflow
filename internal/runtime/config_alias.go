package runtime

import configpkg "github.com/drblury/protoflow/internal/runtime/config"

// Config re-exports the runtime configuration structure from the config subpackage.
type Config = configpkg.Config
